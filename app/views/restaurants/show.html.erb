<h1>Details on <%= @restaurant.name %></h1>
<h3><%= "⭐" * @restaurant.rating %></h3>
<h4>Address: <%= @restaurant.address %></h4>
<%= link_to "Edit restaurant", edit_restaurant_path(@restaurant.id) %>

<%= link_to "Delete restaurant", delete_restaurant_path(@restaurant.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>

<%= link_to "To home", restaurants_path %>
